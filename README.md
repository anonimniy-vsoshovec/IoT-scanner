# IoT Scanner - Система безопасности IoT устройств

Полнофункциональное fullstack приложение для сканирования IoT устройств в сети и обнаружения уязвимостей безопасности.

## 🚀 Технологии

### Frontend

- **Next.js 14** (App Router)
- **TypeScript**
- **TailwindCSS**
- **ShadCN UI** компоненты
- **React 18**

### Backend

- **FastAPI**
- **SQLAlchemy** (ORM)
- **PostgreSQL**
- **Python-nmap** для сканирования сети

### Инфраструктура

- **Docker** & **Docker Compose**
- **PostgreSQL 15**

## 📋 Возможности

- 🔍 **Сканирование сети** - автоматическое обнаружение IoT устройств в указанной сети
- 🛡️ **Анализ уязвимостей** - проверка безопасности устройств
- 📊 **Детальная информация** - просмотр портов, сервисов, типов устройств
- 💡 **Рекомендации** - конкретные инструкции по устранению уязвимостей
- 📱 **Современный UI** - удобный интерфейс с адаптивным дизайном

## 🏃 Быстрый старт

### Требования

- Docker и Docker Compose
- (Опционально) Для локальной разработки: Node.js 20+, Python 3.11+

### Запуск с Docker

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd "IoT scanner"
```

1. Запустите приложение:

```bash
docker-compose up --build
```

1. Приложение будет доступно:
    - Frontend: <http://localhost:3000>
    - Backend API: <http://localhost:8000>
    - API документация: <http://localhost:8000/docs>

### Локальная разработка

#### Backend

1. Создайте виртуальное окружение:

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

1. Убедитесь, что PostgreSQL запущен (или используйте Docker для БД):

```bash
docker-compose up postgres -d
```

1. Запустите backend:

```bash
uvicorn main:app --reload
```

#### Frontend

1. Установите зависимости:

```bash
cd frontend
npm install
```

1. Запустите dev сервер:

```bash
npm run dev
```

## 📖 Использование

### Запуск сканирования

1. На главной странице введите сеть для сканирования в формате CIDR (например: `192.168.1.0/24`)
2. Опционально укажите название сканирования
3. Нажмите "Запустить сканирование"
4. Дождитесь завершения сканирования (статус можно отслеживать на главной странице)

### Просмотр устройств

1. Перейдите в раздел "Устройства" для просмотра всех обнаруженных устройств
2. Кликните на устройство для просмотра детальной информации
3. На странице устройства вы увидите:
    - Информацию об устройстве (IP, MAC, тип, производитель)
    - Список открытых портов
    - Обнаруженные уязвимости с рекомендациями по исправлению

### Работа с уязвимостями

- Каждая уязвимость содержит описание и рекомендации по устранению
- Вы можете отметить уязвимость как исправленную после принятия мер
- Уязвимости отображаются с указанием уровня критичности (низкая, средняя, высокая, критическая)

## 🔒 Обнаруживаемые уязвимости

Система обнаруживает следующие типы уязвимостей:

- Открытые незащищенные порты (Telnet, FTP, HTTP)
- Устаревшие версии протоколов (SSH, SMB)
- UPnP конфигурации
- Стандартные учетные данные (для IoT устройств)
- Большое количество открытых портов

## ⚠️ Важные замечания

1. **Права доступа**: Для сканирования сети требуются права root/sudo (в Docker контейнере это настроено автоматически)

2. **Безопасность**: Это приложение предназначено для сканирования **вашей собственной сети**. Не используйте для сканирования чужих сетей без разрешения.

3. **Производительность**: Сканирование большой сети (например, /16) может занять длительное время.

4. **Среда разработки**: Для разработки рекомендуется использовать Docker только для PostgreSQL, а backend и frontend запускать локально.

## 📝 API Документация

После запуска backend, полная документация API доступна по адресу:

- Swagger UI: <http://localhost:8000/docs>
- ReDoc: <http://localhost:8000/redoc>

## 🐛 Решение проблем

### Backend не может подключиться к БД

- Убедитесь, что PostgreSQL запущен
- Проверьте переменные окружения DATABASE_URL

### Сканирование не работает

- Убедитесь, что nmap установлен (в Docker это настроено автоматически)
- Проверьте права доступа для сетевого сканирования

### Frontend не подключается к API

- Проверьте, что backend запущен на порту 8000
- Проверьте переменную окружения NEXT_PUBLIC_API_URL

## 📄 Лицензия

Этот проект создан в образовательных целях для проекта по информационной безопасности.
